# CLAUDE.md — LeaveEasy

ระบบขอลาออนไลน์ · HTML/CSS/JS ล้วน ไม่มีขั้นตอน build · ฐานข้อมูล Firebase Firestore
เปิดผ่าน `http://localhost:...` เท่านั้น (เปิดแบบ `file://` เบราว์เซอร์จะบล็อก ES module)

---

## 1. ชื่อโฟลเดอร์ (collection) ทั้งหมดใน Firestore

มี 3 โฟลเดอร์หลัก + 2 โฟลเดอร์ย่อย ชื่อสะกดตรงตัว ตัวพิมพ์เล็ก/ใหญ่สำคัญ

| เส้นทาง | ชื่อไฟล์ (document id) | เก็บอะไร |
|---|---|---|
| `users` | `u001`, `u002`, `u003` | ผู้ใช้ · role = `employee` / `manager` / `hr` |
| `leaveTypes` | `lt001`, `lt002`, `lt003` | ประเภทการลา (ลาพักร้อน · ลาป่วย · ลากิจ) |
| `leaveRequests` | `lr001` … `lr005` | ใบขอลา (มีช่อง `aiSuggestion` เก็บสรุปล่าสุดจากปุ่ม AI ด้วย ดูหัวข้อ 4) |
| `leaveRequests/{requestId}/approvals` | `ap001` … `ap004` | ความเห็นการอนุมัติ — เป็น **subcollection** ซ้อนอยู่ในใบลาแต่ละใบ ไม่ใช่ collection ระดับบนสุด |
| `leaveRequests/{requestId}/aiLog` | สุ่มโดย Firestore | ประวัติการเรียก AI ทุกครั้ง (`input`, `output`, `createdAt`) — **subcollection** เช่นกัน ดูหัวข้อ 4 |

กติกาที่ใช้ทั้งโปรเจกต์:

- ตั้งชื่อไฟล์เองด้วย `setDoc(doc(db, ...), ...)` สำหรับข้อมูลที่กำหนด id ตายตัวได้ (เช่น seed) — **ห้ามใช้ `addDoc`** ที่สุ่มชื่อให้กับข้อมูลแบบนั้น เพราะต้องการให้กดใส่ข้อมูลซ้ำแล้วเป็นการเขียนทับ ไม่ใช่เพิ่มใหม่
  ข้อยกเว้น: เอกสารที่ต้อง "เพิ่มรายการใหม่ทุกครั้ง" จริง ๆ (ใบลาที่ผู้ใช้ยื่นเอง, ประวัติ `aiLog`) ใช้ `addDoc` ให้ Firestore สุ่ม id ให้ได้ตามปกติ เพราะไม่มีความหมายจะ "เขียนทับของเดิม"
- **ไม่เก็บช่อง `id` ลงในไฟล์** เพราะชื่อไฟล์คือ id อยู่แล้ว และ `approvals` ตัดช่อง `requestId` ออกด้วย เพราะตำแหน่งที่เก็บบอกอยู่แล้วว่าเป็นของใบไหน
- Firestore ไม่มี JOIN — ทุกใบลาจึงเก็บ **ชื่อคู่กับรหัสเสมอ** (`requesterId` + `requesterName`, `approverId` + `approverName`, `leaveTypeId` + `leaveTypeName`) ไม่งั้นหน้าจอจะขึ้น `u001` แทนชื่อคน

ข้อมูลตัวอย่างต้นทางอยู่ที่ [js/data.js](js/data.js) และสคริปต์ที่ส่งขึ้น Firestore อยู่ที่ [js/seed.js](js/seed.js) — ถ้าแก้โครงสร้าง ต้องแก้ให้ตรงกันทั้งสองไฟล์

---

## 2. สถานะใบลา 3 แบบ

ช่อง `status` ใน `leaveRequests` มีได้ **3 ค่าเท่านั้น** และเก็บเป็น **ข้อความภาษาไทย** ตรงตัว:

| ค่า | ความหมาย |
|---|---|
| `รอพิจารณา` | ยื่นแล้ว ยังไม่มีคนตัดสิน — ค่าเริ่มต้นของใบลาที่สร้างใหม่ |
| `อนุมัติ` | ผู้อนุมัติเห็นชอบแล้ว |
| `ไม่อนุมัติ` | ผู้อนุมัติปฏิเสธ |

ข้อควรระวัง:

- **ห้ามใช้ค่าอังกฤษ** (`pending` / `approved` / `rejected`) หรือค่าอื่นนอกเหนือจาก 3 ค่านี้ — หน้าจอกรองและนับจำนวนจากข้อความไทยตรง ๆ
- สะกดให้ตรงเป๊ะ ไม่มีเว้นวรรค ไม่มีคำต่อท้าย
- ใบที่ยัง `รอพิจารณา` อาจยังไม่มีผู้อนุมัติ — `approverId` / `approverName` เป็นสตริงว่างได้ (ดู `lr003`)

---

## 3. ข้อห้าม — ห้ามใส่คีย์ลงไฟล์ที่ push

**ห้ามเขียน API key, รหัสผ่าน, service account, token หรือความลับใด ๆ ลงในไฟล์ที่จะ commit/push ขึ้น repo**

- ห้าม hard-code ค่าลับลงในไฟล์ `js/*.js`, `*.html` หรือไฟล์ตั้งค่าใด ๆ ที่ถูก track
- ห้ามวางค่าลับไว้ในข้อความ commit หรือใน README
- ถ้าจำเป็นต้องใช้ค่าลับ ให้เก็บไว้ในไฟล์นอก repo แล้วเพิ่มชื่อไฟล์นั้นลง `.gitignore` ก่อนเสมอ
- ถ้าเผลอ push ไปแล้ว: ให้ถือว่าคีย์นั้น **รั่วแล้ว** — ต้องไป revoke/สร้างใหม่ใน Firebase Console ก่อน ไม่ใช่แค่ลบบรรทัดออกแล้ว commit ทับ เพราะค่าเดิมยังอยู่ในประวัติ git

⚠️ สถานะปัจจุบัน: [js/firebase.js](js/firebase.js) ยังมี `firebaseConfig` พร้อม `apiKey` เขียนไว้ตรง ๆ ในไฟล์ ก่อน push สาธารณะให้ย้ายออกหรือยืนยันว่ารับความเสี่ยงได้ และต้องปิดท้ายด้วยการตั้ง **Firestore Security Rules** ให้แน่นเสมอ (ตอนนี้ใช้ test mode ซึ่งเปิดให้ใครก็เขียนได้)

---

## 4. ฟีเจอร์ผู้ช่วย AI (ผ่าน OpenRouter)

มีปุ่ม AI 2 จุดในระบบ เรียกผ่านฟังก์ชันกลาง `เรียกAI()` ใน [js/ai.js](js/ai.js) ซึ่งดึงคีย์จาก `js/config.local.js` (ไฟล์นี้ไม่ push ขึ้น GitHub ดูหัวข้อ 3):

| จุดที่ใช้ | ทำอะไร | เขียนอะไรลง Firestore |
|---|---|---|
| หน้ายื่นใบลาใหม่ — ปุ่ม "ให้ AI ช่วยจัดประเภทการลา" | อ่านช่องเหตุผล + รายชื่อประเภทที่มีจริง แล้วเลือกประเภทให้ | **ไม่เขียนอะไรเลย** แค่เติมค่าในฟอร์มที่ยังไม่ได้บันทึก ผู้ใช้ต้องกด "บันทึก" เองอีกที |
| หน้ารายละเอียดใบลา — ปุ่ม "ให้ AI ช่วยสรุปใบลา" | อ่านข้อมูลใบลาใบนั้น แล้วให้ AI เขียนสรุปสั้น ๆ | เขียนช่อง **`aiSuggestion`** (สตริง เก็บสรุปล่าสุดค่าเดียว เขียนทับทุกครั้งที่กดใหม่) ในเอกสาร `leaveRequests/{id}` เอง + เพิ่มเอกสารใหม่ 1 รายการในโฟลเดอร์ย่อย **`leaveRequests/{id}/aiLog`** ทุกครั้งที่เรียก เก็บ `input` (prompt ที่ส่งไป) `output` (คำตอบดิบจาก AI) `createdAt` — เป็นประวัติสะสม ไม่เขียนทับ |

กติกาตายตัวของฟีเจอร์ AI ทุกจุดในระบบ:

- **ห้าม AI แตะช่อง `status` เด็ดขาด** ไม่ว่าจะทางตรงหรือทางอ้อม — สถานะใบลา (`รอพิจารณา` / `อนุมัติ` / `ไม่อนุมัติ`) เปลี่ยนได้ทางเดียวคือคนกดปุ่ม "อนุมัติ" / "ไม่อนุมัติ" เองเท่านั้น (ดู `เปลี่ยนสถานะ()` ใน `js/leave-request-detail.js`) AI มีหน้าที่แค่ "เสนอ" ข้อมูลให้คนอ่านประกอบการตัดสินใจ ไม่ใช่ตัดสินใจแทน — กติกาข้อนี้เข้มกว่าข้ออื่น ห้ามผ่อนแม้จะดูสมเหตุสมผลแค่ไหนก็ตาม
- ผลลัพธ์ AI ที่จะไปมีผลกับ "การตัดสินใจของคน" ต้องให้คนยืนยันก่อนเสมอ ไม่ใช่แค่แสดงแล้วจบ: ปุ่มจัดประเภทการลาแค่เติมค่าในฟอร์มที่ยังไม่ได้บันทึก ผู้ใช้ต้องกด "บันทึกใบลา" เองอีกทีของจริงจึงจะลงฐานข้อมูล — ส่วน `aiSuggestion` (สรุปให้หัวหน้าอ่าน) จะเขียนลง Firestore ทันทีที่ AI ตอบกลับ **โดยไม่ต้องกดยืนยันแยก** เพราะตัวมันเองเป็นแค่ "ข้อมูลประกอบให้อ่าน" ไม่ใช่การตัดสินใจ (คนยังต้องกดอนุมัติ/ไม่อนุมัติเองอยู่ดี ไม่ว่า `aiSuggestion` จะเขียนสำเร็จหรือไม่) ถ้าจะเปลี่ยนให้ `aiSuggestion` ต้องผ่านการยืนยันก่อนเขียนด้วย ต้องพูดชัดเจนแยกต่างหาก
- เรียก AI ไม่สำเร็จหรือช้าเกิน 15 วินาที **ต้องไม่ทำให้หน้าเว็บค้าง** และงานหลักของหน้านั้น (บันทึกใบลา / อนุมัติ-ไม่อนุมัติ) ต้องยังทำต่อได้ตามปกติ
